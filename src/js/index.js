// Import modules
import { RecipeList } from './modules/recipe/recipe-list.js';

// Initialize app
document.addEventListener('DOMContentLoaded', () => {
    console.log('Recipe Viewer application initialized');
    new RecipeList();
}); 